[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    smoke: Quick health and connectivity checks (run these first)
    integration: Full end-to-end tests requiring complete stack
addopts = -v --tb=short
